import { StickyClassDirective } from './sticky-class.directive';

describe('StickyClassDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyClassDirective();
    expect(directive).toBeTruthy();
  });
});
